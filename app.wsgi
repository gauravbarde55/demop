import sys
import os

# Add the directory containing your app to the Python path
sys.path.insert(0, r"E:\Inetpub\Bsebti\BSEBTI_New_Latest\demop")

from app import app
# from app import app as application  # Ensure this matches your file and app name
